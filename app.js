// (I) Import ExpressJS library

// (II) Create a variable `app` to run express

// (III) Configure the router (or routes) here.

// (IV) Configure the PORT using environment variables from OS (process)

// (V) Set the `listen` function to run our application
